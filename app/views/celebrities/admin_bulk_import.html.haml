.row
  .span12
    %h2 Bulk Import

.row
  .span12
    = form_tag(celebrities_admin_bulk_import_post_path, :multipart => true ) do
      = label_tag "csv file"
      = file_field_tag 'csv_file'
      = submit_tag "submit", class: 'btn btn-primary'
